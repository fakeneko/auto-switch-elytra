# Automatically build the project and run any configured tests for every push
# and submitted pull request. This can help catch issues that only occur on
# certain platforms or Java versions, and provides a first line of defence
# against bad commits.

name: build & release

permissions:
  contents: write

on:
  push:
    tags:
      - '*'

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: checkout auto_switch_elytra
        uses: actions/checkout@v6
        with:
          path: auto_switch_elytra

      - name: checkout fakeneko_config
        uses: actions/checkout@v6
        with:
          repository: fakeneko/fakeneko_config
          path: fakeneko_config

      - name: setup jdk
        uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: 'microsoft'

      - name: setup gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.5.0'

      - name: build
        working-directory: ./auto_switch_elytra
        run: gradle build

      - name: collect release artifacts
        working-directory: ./auto_switch_elytra
        run: |
          mkdir -p release
          cp fabric/build/libs/*fabric*[0-9].jar release/ 2>/dev/null || true
          cp neoforge/build/libs/*neoforge*[0-9].jar release/ 2>/dev/null || true

      - name: create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
          files: auto_switch_elytra/release/*.jar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
